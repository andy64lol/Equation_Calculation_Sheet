[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Equation_Calculation_Sheet"
version = "1.0.0"
description = "Simple DSL for equations"
authors = [{name="Andy"}]
readme = "README.md"
requires-python = ">=3.9"
dependencies = []
